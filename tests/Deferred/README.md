# Tes Ditunda (pembayaran)

Tes di folder ini mengikuti fitur tagihan yang **tidak aktif di MVP**. Tidak dijalankan oleh `phpunit.xml` (hanya `tests/Unit` dan `tests/Feature`).

Lihat juga [../deferred/README.md](../deferred/README.md).
